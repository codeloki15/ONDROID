# On-device model binaries not currently used by the app (FunctionGemma path is
# dormant) — kokoro-en-v0_19/model.onnx+voices.bin, kws/*.onnx and the sherpa AAR
# ARE tracked, via Git LFS (see .gitattributes).
app/src/main/assets/models/*.litertlm
app/src/main/assets/models/*.task

# Local model download cache (outside-tree convenience)
_models_cache/

# Cloned third-party repos kept locally for reference only (not part of the app)
externals/
UI_images_reference/
docs/

# FunctionGemma weights (on-device tool-routing model, currently dormant) — 1.6GB,
# exceeds GitHub's 100MB blob limit. Kept locally outside the repo.
_fg_hf/
_fg_litertlm/

# Python server venv (legacy server, not part of standalone app)
server/venv/

# Gradle / Android build outputs
.gradle/
build/
app/build/
*.iml

# IDE / local
.idea/
local.properties
.DS_Store
**/.DS_Store
tools/wakeword-training/venv/
tools/wakeword-training/piper-sample-generator/
tools/wakeword-training/data/
tools/wakeword-training/generated/

# Internal/dev-only content kept local (not part of the public repo)
server/
scripts/
ANDROID_SECURITY.md
APK_file/
